import React from 'react';
import { UnifiedRoleProfile } from '../components/UnifiedRoleProfile';

const FoProfile: React.FC = () => {
  return <UnifiedRoleProfile role="FO" pageTitle="Profile" subtitle="Manage your account details and security settings." />;
};

export default FoProfile;
