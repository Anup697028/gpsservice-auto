{
  "name": "gps-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "backfill:rh-approved": "node scripts/backfill-rh-approved-flags.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.0.5",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/nodemailer": "^7.0.9",
    "typescript": "^5.6.3"
  }
}
