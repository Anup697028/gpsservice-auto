import React from 'react';
import { UnifiedRoleProfile } from '../components/UnifiedRoleProfile';

const RhProfile: React.FC = () => {
  return <UnifiedRoleProfile role="RH" pageTitle="Profile" subtitle="Manage your account details and regional leadership settings." />;
};

export default RhProfile;
